import styled from "styled-components";

const Footer = styled.span`
  position: fixed;
  bottom: 0px;
  text-decoration: underline;
`;

export default () => {
  return <Footer>Created by Pedro Nestor</Footer>;
};
